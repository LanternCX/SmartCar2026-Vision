[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    unit: host-side deterministic unit tests
    contract: documentation and behavior contract tests
    hil: hardware-in-loop manual or scripted validation
